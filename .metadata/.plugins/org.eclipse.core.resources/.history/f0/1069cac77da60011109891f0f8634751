#include "open_interface.h"
#include "Timer.h"
#include "movement.h"


void main(){
    oi_t *sensor_data = oi_alloc();
    oi_init(sensor_data);


    turn_clockwise(sensor_data, 90);
    timer_waitMillis(500);

    oi_free(sensor_data);
}
